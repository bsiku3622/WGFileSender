<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Only the updates subdir is shared with the package installer, not all of cacheDir. -->
    <cache-path name="updates" path="updates/" />
</paths>
